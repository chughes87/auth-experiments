services:
  postgres:
    image: postgres:16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_USER: auth_dev
      POSTGRES_PASSWORD: auth_dev
      POSTGRES_DB: auth_experiments
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
